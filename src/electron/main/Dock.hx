package electron.main;

@:jsRequire("browser-window")
extern class Dock {
  // TODO
  // bounce([type])
  // cancelBounce(id)
  // setBadge(text)
  // getBadge()
  // hide()
  // show()
  // setMenu(menu)
}
